<h1><%= @campaign.name %></h1>
<h2>Results:</h2>
<ul>
  <% @results.each do |choice, count| %>
    <li><%= choice %>: <%= count %> votes</li>
  <% end %>
</ul>
<p>Invalid votes: <%= @invalid_count %></p>
